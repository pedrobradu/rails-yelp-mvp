<h1>Faça uma review do restaurant:</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: (0..5) %>
  <%= f.input :content %>
  <%= f.submit "Criar a Review!" %>
<% end %>